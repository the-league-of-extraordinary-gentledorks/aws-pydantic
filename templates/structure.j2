class {{ shape_name }}(_{{metadata.serviceId}}Base):
{% if members %}
{% for member in members %}
    {{ member | to_snake }}: {%if members[member].ref.is_atomic or members[member].ref.is_list%}{{members[member].shape}}{%else%}'{{members[member].shape}}'{%endif+%} = pydantic.Field(None, alias="{{member}}")
{% endfor +%}
{% else %}
    pass
{% endif %}
